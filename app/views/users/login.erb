<div class="jumbotron" id="edit"><p>
<h3>Login</h3>
<% if @user && @user.errors.any? %>
  <div>
    <strong>Some issues with your info, please fix them:</strong>
    <ul>
      <% @user.errors.full_messages.each do |user| %>
        <li><%= user %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<form action='/users/login', method='post'>
  <div>
    <label>Email:</label>
    <input type="text" name="email" size="60" value="<%= @user && @user.email %>">
  </div>
  <div>
    <label>Password:</label>
    <input type="password" name="password" size="55" value="<%= @user && @user.password %>">
  </div>
  <div>
    <input type="submit" value="Submit">
  </div>

</form></p></div>